using static System.Console;
using static System.Math;
using static System.DateTime;


class NameSpaceUsing
{
    static void Main()
    {
        var aq = Sqrt(10 * 10);
        WriteLine(aq);

        WriteLine(Now);
    }
}